# Lesson 03: Raising Custom Exceptions

Learn to design domain-specific exceptions and error messages.

Exercises:
- Implement `validate_username(name)` raising `ValueError` with helpful message.
- Implement `require_positive(n)` raising `ValueError`.
- Implement `parse_port(s)` converting string to int and validating range 1..65535.


